<!-- codex-memory:template=task-decisions:v1 -->

# 决策

- SpaceMember 继续只表示人类，AgentGrant 继续表示 Agent 成员，不新增表或迁移。
- 添加入口使用同一弹窗的“用户/智能体”状态，不新增独立按钮。
- 候选只来自当前用户的 active、未撤销、尚未加入该 Space 的 Agent。
- Agent 角色仅 viewer/editor，并自动应用设计中固定的默认 Space scopes。
- 新 Grant 服务端校验 Agent 归调用者所有；管理员对已有 Agent Grant 的调整和移除不受 Agent 所有权限制。
- 人类邮箱添加流程和现有 Agent 权限卡片不得回归。
