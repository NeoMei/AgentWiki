# 决策

- 保留 Space 自定义模板，将它作为来源筛选，不单独增加创建方式。
- 复用现有模板版本、预览和实例化后端接口及会话逻辑。页面组优先打开创建的根目录，单页打开编辑器。
- 新建 URL 携带 folder；from=collaboration 仅指定同 Space 协作返回入口，不接受任意外部返回 URL。
- 页面模式返回交由导航守卫确认；卸载才中止进行中的请求，避免取消离开后卡住。用户实际编辑后切换创建方式也需确认。
- 保持现有产品组件与蓝色操作风格，无新增组件库或运行时依赖，无服务器/数据库/同步变更。

- 2026-09-10继续执行v0.11.1应用发布；同步包和插件版本独立，保持不变。Space模板统计以有效Space为准；保留能力及既有页面组allowlist。
