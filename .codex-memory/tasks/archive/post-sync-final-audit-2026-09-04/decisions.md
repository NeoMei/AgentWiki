# 决策

- 先 fetch 并以 `origin/master@36e70c5` 为真实远端基线，通过 `79ac85c` 合并，不覆盖原始脏工作区。
- Browser skill/plugin 不可用时，使用仓库 Playwright 驱动真实 installed Chrome，并保留持久回归用例。
- 产品修复先观察正确 RED，再做最小根因修复；环境参数错误与产品缺陷分开记录。
- 5000 页超时修复二次方写入根因，不仅放宽事务超时。
- 数据服务使用任务专属 55433/56380，清理仅按完整容器名处理，不触碰 55432 其他任务。
- 未获 push/发布/部署授权，因此仅将 Mac 本地候选收口为 PASS。
