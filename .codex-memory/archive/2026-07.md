# 2026-07 history

## AgentWiki full remediation

- Replaced the initial assessment-only state with a completed P0-P6 implementation.
- Closed authorization, identity, API-key, local-path, Git-fetch, queue, WebSocket, CORS, rate-limit and audit gaps.
- Added Agent control plane, unified knowledge ingestion, evidence, review/publish/revert, scoped Agent memory and MCP integration.
- Rebased product/design documentation on the current React/Vite architecture and removed false guide/product claims.
- Repaired test configuration, added security/domain tests, ESLint and package-level build gates.
- Validated all 12 migrations from an empty temporary schema, restored the exact checksum-matching missing historical migration, performed API/Worker runtime smoke tests, and completed desktop/mobile UI QA. Three new migrations remain intentionally undeployed on the configured remote database pending an explicit deployment/backup step.
- A second requirements audit found and closed Agent REST approval bypasses, partial-approval publication, indexing/cancellation gaps, missing Git file snapshots, missing effective MCP access display, and mobile space-header crowding.

## AgentWiki deep gap closure

- Closed sensitive search projection, current-user JWT/WebSocket validation, Memory cross-space mutation and concurrent deduplication.
- Added Review publish/revert claiming, hierarchy-cycle and stale-candidate protection, complete PageVersion restore data, origin/latest provenance, relation evidence cleanup and reversible relation archival.
- Rebuilt ingestion around per-source segments, precise chunks/evidence, entity/relation extraction, incremental Page/Relation ChangeItems, durable lexical indexing, renewable worker leases, periodic expired-lease recovery and multi-stage credential revalidation.
- Added DNS-pinned public URL fetching, supported-file allowlisting, MCP capability-level audit without argument values, unified six-entry Space navigation, lazy Review evidence and richer page provenance.
- Added client Vitest/Testing Library and Playwright QA; final code gates passed with 56 server tests, 2 client tests, lint, typecheck and production build. The 13-migration chain validates to an empty schema diff.
- Four migrations remain intentionally undeployed on the configured remote database until a recoverable backup is confirmed.

## AgentWiki remote direct deployment

- Created and checksum-recorded a PostgreSQL 18.4 logical backup, verified its TOC and restored all application objects/data into an isolated temporary database while excluding only the cluster-admin-installed `vector` extension declaration.
- Applied all four pending migrations; the remote database is now at 13/13 with no pending migration.
- Preserved the existing no-Docker runtime. Added user-level systemd supervision for API, independent ingestion Worker and Vite production frontend, plus source mirror-sync to prevent deleted legacy files remaining on the host.
- Added `/api/health`, fixed runtime-only McpModule and Worker/Search module dependency boundaries, and added complete API/Worker Nest module-graph tests.
- Completed real remote smoke for registration, Space/Page/Search, Agent credential/grant, MCP read and audit, Source/Run, ChangeSet approval/publication and Page provenance; removed all temporary data afterward.
- Final gates: 16 Jest suites/58 server tests, 2 client tests, lint, typecheck and production builds. All three remote services are active with zero restarts; Docker container count is zero.

## Single-workspace editor and bilingual interface

- Replaced the default split Markdown editor with one Obsidian-like workspace that switches between Edit and Preview while preserving collaboration, save state and version history; added `Ctrl/⌘ + E` switching.
- Added a shared Chinese/English language context, persisted preference, document language synchronization and language switchers for authenticated and public surfaces.
- Localized the global/space navigation and the principal authentication, Space/Page, Agent/Memory, Source/Run, Review, Graph, Members, Profile, MCP integration, product and guide workflows. Corrected product copy that previously claimed split-screen editing.
- Added focused language and Markdown workspace component tests and durable Playwright desktop/mobile browser coverage with temporary API fixtures and automatic cleanup.
- Published only the client through the existing native systemd workflow with a remote client backup; no Docker was introduced. Remote build, API/DB/Redis health, three service states and the served asset fingerprint were verified.
- Final frontend gates: ESLint, 4/4 Vitest tests, production Vite build and 2/2 remote Playwright tests. Desktop verified single-surface switching and language persistence; mobile verified switching without horizontal overflow.
