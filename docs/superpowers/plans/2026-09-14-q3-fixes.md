# Q3 manual regression fixes implementation plan

**Goal:** Resolve all twenty items from the approved Q3 assessment, preserving user edits, authorization and synchronization integrity.
**Architecture:** Repair existing React/NestJS and independent Obsidian plugin paths; preserve the current UI system and public sync contracts. Add behavioral regression coverage for functional changes. Do not migrate user data or weaken concurrency/verification guards.
**Spec:** `/Users/neomei/项目/codexprojects/AgentWiki /测试报告/AgentWikiQ 3/修复评估-2026-09-14.md`

## Global constraints

- All product code under `agentwiki/`; plugin task uses `/Users/neomei/项目/codexprojects/AgentWiki-Obsidian-q3-fixes`.
- No production writes, release, push or installing into daily Vault. Keep existing pages, immutable templates and credentials intact.
- Chinese and English display text; preserve technical IDs and permissions. Main checkout path has trailing space; Git always explicit `--work-tree`.
- Workers have disjoint file ownership. No commits concurrently; report changed files for coordinator commit. No worker spawns other agents. Read applicable AGENTS and required skills, use regression-first behavior checks.
- Each task: run existing focused baseline, add reproducer, observe expected failure, implement minimal repair, run scoped tests and typecheck, self-review, write report with limitations. Evidence must distinguish mock/UI/DB/native and never claim unavailable Windows evidence.

### Task 1: Publication conflict and search correctness

Owner: server worker. Files: `agentwiki/apps/server/src/review/**`, `agentwiki/apps/server/src/core/search/**`, related tests. Search UI integration owned by Task 2. Existing knowledge producer baselines may be read, request expansion if repair requires them.

- [ ] Audit update proposal producers, reproduce candidate→human edit→publish overwrite with missing baseline and stale baseline. Existing `publishPageUpdateLocked` guards are conditional; publish-time CAS is not submission-time CAS. Reject unverifiable updates, preserve working producer baselines and collaboration behavior. Do not stamp a fresh baseline on a stale candidate at publish time. Include archive if the same omission permits loss.
- [ ] Check review retry state after conflict and ensure no accidental partial approval/publication. Verify current-baseline success and no historical page loss.
- [ ] Search: run lexical and semantic authorized matching together, deduplicate by page ID, favor exact/substring title hits without losing semantic-only results; apply limit after combination. Do not label arbitrary text matching as 100% semantic similarity. Expose optional matchType (`text` or `semantic`) for UI, preserving numeric similarity compatibility. Test 3 title matches where semantic returns only one, no embedding, empty authorization, mixed results, limit, inaccessible pages.
- [ ] Report to `.superpowers/q3/task-1-report.md` with exact tests, root cause and residual risks.

### Task 2: Login, review, editor and collaboration UI correctness

Owner: UI worker. Files: `agentwiki/apps/client/src` EXCEPT `features/knowledge/**`; `agentwiki/apps/server/src/core/dto/page.dto.ts` and its tests only on server. No server review/search files. Own shared language dictionary.

- [ ] Fix forced password page Navbar review-count requests/401 logout race without keeping invalid sessions; test required password change route remains usable and polling resumes for normal users.
- [ ] Revert action obtains correct space content-tree revision and sends expectedTreeRevision, protects duplicate submits and concurrent changes, keeps v3 legacy revert unavailable. Add frontend API contract regression.
- [ ] Stabilize editable role and input-key row identity across typing/paste; meaningful focus-preserving tests. Correct collaboration manage-template navigation, keeping page-template management explicitly distinct.
- [ ] Success Toast auto-dismiss with stable timer and cleanup; errors remain available. Localize permission errors, Agent role display and system collaboration task names/objectives without modifying user prose or historical template storage. Update stale guide screenshot flow to source-backed live instructions or current visuals, consistent with current connection page.
- [ ] Title field clear accessible label/placeholder/focus border; reject all-whitespace title through DTO and UI, display fallback title/minimum click area on existing blank directory rows. Do not rename historical user pages.
- [ ] Search UI honors optional matchType from Task 1: text match label vs semantic relevance, no misleading uniform 100%. Remain compatible with old API response.
- [ ] Add duplicate-content advisory in review if feasible without new endpoint: do not hard-block legitimate identical content or imply an exhaustive global detector. Coordinate server interface if needed rather than inventing unsupported APIs.
- [ ] Report `.superpowers/q3/task-2-report.md`, include browser-oriented regressions, touched files and any scope expansion request.

### Task 3: Graph navigation

Owner: graph worker. Files only `agentwiki/apps/client/src/features/knowledge/**`.

- [ ] Add canvas zoom anchored on cursor, background pan and node drag with consistent render/hit-test transform, preserving click-select and double-click-open. Avoid navigation after drag. Include fit/reset controls, sensible bounded zoom, container resize fit, mouse and pointer lifecycle cleanup.
- [ ] Preserve accessibility selector, bilingual copy inline via existing zh convention (do not edit shared LanguageContext owned Task 2). Keep current graph layout/relationship behavior.
- [ ] Tests exercise pan/zoom/drag and transformed selection, not just source tokens; run existing graph suite. Report `.superpowers/q3/task-3-report.md`.

### Task 4: Obsidian control storage, merge verification and deletion recovery

Owner: plugin worker. Worktree `/Users/neomei/项目/codexprojects/AgentWiki-Obsidian-q3-fixes`; main repo read only. Read `docs/agentwiki-context.md` and project instructions.

- [ ] Reproduce Q3 new 3: first pull on extra mapped Space, Sync v3, “未知或未来的控制 payload 版本”. Audit MutableControlRepository guards/callers for supported-but-rejected v3 payload, cross-map state and actual future/corrupt distinction. Fix proven root cause, preserve main/prev/next and unknown versions fail-closed. No deleting baseline to make it pass.
- [ ] Reproduce Q3 new 4: both sides edited then choose local → V3_VAULT_VERIFY_FAILED. Audit apply/scan content hashes, Windows path/reference normalization and identity state. Fix root cause with representative fixture; report precise actionable verification mismatch without weakening validation or logging document contents.
- [ ] Q3 new 5 is Sync v2 local deletion of 2 folders/5 pages with unchanged remote. Confirm automatic merge handles deletion intentionally; restore-server option must actually recover if explicitly selected, and unexpected execution failures must retain evidence. Fix confirmed deletion/recovery defects and clear UI semantics; do not make auto merge silently restore all deletions.
- [ ] Run focused and full plugin tests/build, preserve cross-platform boundaries. No deployment/installation. Write report `/Users/neomei/项目/codexprojects/AgentWiki /.worktrees/q3-fixes-20260914/.superpowers/q3/task-4-report.md` and list changed files.

### Task 5: Git source and multi-space access

Owner: coordinator. Files server knowledge-pipeline source/remote-source, authorization and local-sync gateway plus related tests; UI ownership Task 2.

- [ ] Inspect report Git failed run, source stage/error mapping and git implementation. Use isolated repo/network read-only reproduction to determine actual defect; improve only proven defects and actionable error reporting. Never bypass SSRF validation.
- [ ] Trace effective Agent grants and credential scopes through gateway list_spaces, compare direct MCP response semantics vs local workspace filtering. Add regression for five granted spaces and scoped credentials; fix any proven mismatch without widening access.
- [ ] Record reproducible outcomes and exact unavailable tester evidence. Coordinate only needed UI labels with Task 2.

### Task 6: Integrated verification and review

- [ ] Review each worker diff for spec and quality, fix findings, then run main test harness/typecheck/lint/build and plugin suite on combined candidate.
- [ ] Exercise repaired UI flows in a local browser fixture, using isolated data; run DB conflict test where local test DB available. Preserve evidence.
- [ ] Independent whole-diff review of main and plugin candidates; resolve findings and rerun affected checks. Commit reviewed candidate and update project task state and tester checklist. Keep release, production deployment, native Windows and local plugin install status explicit.
