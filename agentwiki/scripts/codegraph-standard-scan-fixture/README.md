# CodeGraph standard scan fixture

This small repository exists only for the gated real-CodeGraph acceptance test.
The scan must use its filenames and metadata only; its source body is a privacy
sentinel and must never reach AgentWiki snapshots, generated knowledge, previews,
or controlled remote publish payloads.
