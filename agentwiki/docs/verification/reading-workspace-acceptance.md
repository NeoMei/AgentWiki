# 目录与文章工作区本地验收

状态：本地实现、逐任务审查和最终回归已完成。最终代码 `1d1b0a814d446c2638158d51121fb9e84047e2d8` 的已报告R1–R6均已关闭；2026-09-09补齐原生拖拽与移动后保存的浏览器验收。未合并、推送或发布。

## 范围与环境

仅前端呈现、导航和阅读位置。既有Space/Folder/Page模型、权限、目录修订、版本恢复、保存冲突与同步规则保持；原编辑器、蓝色视觉、语言持久化及能力入口继续复用。

- 分支：`codex/reading-workspace-20260908`；基线：`b776b8304d14070255583f0ed064b6d86905e528`。
- 隔离工作树：`/Users/neomei/项目/codexprojects/AgentWiki /.worktrees/reading-workspace-20260908`。主工作区和其他子模块不在本轮修改范围。
- 本地界面 `http://127.0.0.1:5188`，API `http://127.0.0.1:53088/api`；独立PostgreSQL 55438、Redis 56388。全部为合成内容和账户。
- Node24.18.0、pnpm11.9.0；Codex内置浏览器/CUA，1440×1024与390×844；没有新增浏览器依赖。
- 私有fixture和逐项故障证据位于本计划`.superpowers/sdd/2026-09-08-agentwiki-reading-workspace/`，凭据不提交。

## 实际浏览与数据证据

| 项目 | 已观察结果 |
|---|---|
| 多层与分页目录 | 三层folderId祖先、同名页按ID区分；205同级页加新建页均可访问；跨页保留展开分支及子项，原生侧栏滚动2460切页后仍2460 |
| 键盘目录 | Home/End定位首尾、左右箭头展开收起、Enter进入真实folder视图；展开收起不切换正文 |
| 搜索和图谱入口 | 实际搜索深层页面、图谱选中后打开，均恢复产品知识库/使用指南/内容组织/进阶操作与正确选中项 |
| 原生拖拽与移动后保存 | 2026-09-09在内置浏览器从页面图标拖至进阶操作，目标展开、选中项和面包屑更新；原API GET确认folderId为目标ID，随后实际勾选任务并GET确认[x]且目录关系保持 |
| 本文目录 | 使用真实标题ID、排除代码块，默认关闭；桌面与390px浮窗均在视口内；Escape关闭不重置正文并返回触发器焦点 |
| 正文与附件 | 原附件实际渲染1280×720；checkbox勾选/取消后GET分别为[x]/[ ]；空正文不误用元信息标题生成目录 |
| 读写定位 | 阅读→编辑→预览→阅读保持对应段落；长段落、第二个同名标题、预览从A滚动到B再编辑均已实际复测 |
| 离开保护 | 树、空间导航、历史、返回阅读和浏览器POP取消离开，保留草稿、原选中项和缓冲；手机抽屉内取消也保持 |
| 完整路径 | Chrome实际阅读→编辑→保存→服务端GET确认FINAL-FLOW-20260909→预览→历史v5→返回编辑→阅读→来源→浏览器返回文章→本文目录标题跳转通过 |
| 保存与预览 | 预览PATCH计数0；保存后GET含唯一标记且留在编辑；暂停真实保存期间继续输入，GET仅包含已提交快照，新输入仍dirty |
| 409冲突 | 暂停旧PATCH，另一次原接口当前CAS更新后放行，真实409；界面保留草稿和冲突提示，接受远端后采用远端内容 |
| 只读与权限撤销 | reader界面无编辑/新建/删除，目录和本文目录可用；真实PATCH403。撤销成员后新目录请求403清空树，刷新文章403清空正文与空间上下文，随后恢复原成员 |
| 历史 | 从阅读/编辑进入后返回正确来源；临时收起目录后返回恢复；版本预览Escape回原触发器；恢复真实v3后进入编辑，GET正文与v3一致 |
| 新建及目录管理 | 新页POST与GET确认目标分页目录；真实文件夹创建、改名、删除、恢复同ID/名称/parent；弹窗显示准确路径；删除后焦点落在连接中的有效父节点 |
| 空间子页 | 来源、运行记录、成员、设置、页面模板、协作、新建模板、启动向导、图谱实际点击均显示真实Space标题、唯一导航、正确active且无常驻目录；嵌套返回路径保持 |
| 文章加载失败 | Chrome原文章XHR注入ConnectionRefused，显示正文内重试；清除注入并点击重试后错误消失、文章/目录/面包屑恢复 |
| 局部目录失败 | 1071561a下真实进阶分支XHR失败，其他目录/当前正文保留；可打开兄弟Markdown编辑；分支内重试后深层页面恢复、错误清除、当前文章不变 |
| 子页加载失败 | 注入真实Space GET网络中断后业务区仍全宽：1440下错误区与业务区均1408宽且上下排列；清除中断后Retry成功，错误消失、标题与单导航恢复 |
| 手机与双语 | 390px阅读/编辑/历史/目录抽屉/本文目录/协作/向导无文档横向溢出；英文切换刷新持久化，再恢复中文 |

## 自动化及审查状态

| 阶段 | 结果 |
|---|---|
| 基线 | 客户端93文件1270测试通过 |
| Task1–5 | 逐任务测试及独立审查通过，修复后均有限定复审；最后删除焦点修复4文件36测试通过 |
| Task6 | 最终受影响28文件396测试通过，错误恢复修复3文件49测试通过；类型、范围lint及diff-check通过；独立复审通过 |
| Task7最终代码 | 9d66a123客户端101文件1390测试通过（20.26s）；lint、build（11.32s）、仓库typecheck全部exit0；Vite保留既有大chunk提醒 |
| 2026-09-09最终代码 | 1d1b0a81客户端101文件1395测试通过（21.23s）；lint、build（6.60s）、仓库typecheck全部exit0；既有大chunk提醒保持 |
| R5限定复审 | ab461945..1d1b0a81完整六文件修复经独立审查：R5 ADDRESSED / Spec PASS / Quality APPROVED，无新增问题 |
| 前轮最终审查 | R1–R4集中修复1071561a后全部关闭；新增恢复衔接R6经9d66a123修复和限定复审关闭；Spec PASS / Quality APPROVED，无剩余P2 |

## 最终检查命令与证据

在隔离工作树的`agentwiki`目录运行：

```sh
pnpm --filter @agentwiki/client test
pnpm --filter @agentwiki/client lint
pnpm --filter @agentwiki/client build
pnpm typecheck
```

2026-09-09最终日志分别为`/tmp/agentwiki-reading-1d1b0a81-{test,lint,build,typecheck}.log`；前轮日志保留于`/tmp/agentwiki-reading-9d66a123-{test,lint,build,typecheck}.log`。仓库typecheck包含server/client/protocol/Local Sync。额外核对基线至最终代码：server、packages、锁文件无变更，`git --work-tree="$PWD" diff --check`通过。后续交付提交仅整理文档和截图，不改变已验证源代码。

## 最终视觉对照

已与用户选择的方案2修订图核对：保留左目录、阅读宽度、原蓝色视觉和右上角覆盖式本文目录；工具栏、密度和手机溢出符合已定边界。正文样本含标题、表格、列表和长内容。浏览器已有110%缩放，因此viewport override换算后验证CSS宽度1440/390；没有修改用户缩放，结束时已清除临时视口。

- [2026-09-09移动后桌面1440px](assets/reading-workspace-resume-desktop.jpg)
- [桌面1440px](assets/reading-workspace-desktop.jpg)
- [手机390px本文目录](assets/reading-workspace-mobile.jpg)
- [手机390px空间目录抽屉](assets/reading-workspace-mobile-directory.jpg)

截图用于外观对照；保存/冲突/权限/恢复证据分别见上表及集成测试，互不替代。

## 验证边界与过程问题

- 前轮原生移动复测未发出请求，原失败记录保留；2026-09-09从页面图标开始的原生拖拽已成功，服务端目录与后续checkbox写入回读均通过，关闭此验收缺口。不据此宣称所有拖拽起点或浏览器组合均已覆盖。

- 新增当前页删除回归由真实React组件/控制器集成测试验证；最终浏览器仅观察到原不可撤销确认并取消，未把该轮删除记为实际执行。合成页面d2304e2f-01ae-4c59-8e10-1fa7af67714d保留。
- 版本恢复的最终编辑路由及GET已验证；浏览器自动化未单独可靠捕获恢复确认动作，不据此宣称该原生确认动作录制完整。
- 权限撤销证据不代表已经加载的只读正文能被瞬时推送撤回；验证的是后续请求403、目录清除和文章刷新后的拒绝访问。
- 合成Space无可用Agent，启动向导正确停在原有准备条件；没有为了界面测试创建授权Agent或启动真实外部运行。运行详情由既有功能及路由测试覆盖，不能计为外部协作工作流验收。
- 附件及编辑辅助、socket等共享前端路径有回归测试；本轮没有重新执行带真实外部凭据的Agent辅助流式服务。
- 初期目录重复挂载请求、展开子项丢失、加载期间滚动归零、工具栏滤镜引起浮层偏移、段落锚点误差、删除后焦点竞态均保留故障记录，并经过修复和复测。开发期间HMR日志不作为稳定最终代码结论。
- 附件样本曾因扩展名/MIME或引用格式不符合既有校验而被拒绝，修正合成样本后成功；未放宽任何后端校验。
- 仅本地候选；GitHub、npm、生产部署和跨浏览器完整覆盖不属于本轮已验证结果。

## 实施中的协调决定

1. 公共外框先建立最小接口，各任务完成自身集成，避免复制编辑/目录业务；风险是中间提交暂不完整，已在后续任务合拢。
2. 主工作区有其他并行改动，因此使用独立工作树；成本是本地预览与后续合并需明确候选分支。
3. 手机目录抽屉由目录任务一并负责；若归属不当，需要局部响应式布局返工。
4. Task7已知修复由整分支审查承担任务关口；成本是部分反馈延后至最后一轮，实际已纳入集中修复。
5. 最终一轮修复后发现恢复衔接回归，因此按已授权完成要求追加最小R6修复，超出SDD的一波修复流程上限；成本是多一轮有限实现/复审，避免保留已存在的恢复能力回归。

## 2026-09-09续接与R5关闭

- 位置缓存限制100条并按最近使用淘汰；退出登录或切换用户同步清空，同用户令牌刷新保留。旧页面滚动/卸载回调带会话代际检查，不能重新写回清空的缓存。新增容量、切换和真实PagePreview旧监听器回归覆盖，独立限定复审通过。
- 原测试进程已停止，旧启动器随机生成的身份种子与持久化库不匹配。没有覆盖原库或绕过认证；在原测试PG容器创建独立数据库`reading_resume_20260909`，执行既有迁移后生成新合成账户/内容。启动种子仅保存在权限0600的私有运行文件中，不提交凭据。原`reading_test`库保留。
- 续接私有证据位于`.superpowers/sdd/2026-09-08-agentwiki-reading-workspace/resume-20260909/`：`move-readback.json`、`move-checkbox-readback.json`与`r5-review.md`。API回读均200，移动后folderId为目标深层目录，随后勾选保存的updatedAt为`2026-09-09T00:36:11.754Z`。
- 本轮新浏览器验证仅补齐移动及后续任务保存；上表其他真实路径为前轮已完成证据，本轮通过完整自动化回归覆盖。外部Agent运行等上述边界继续有效。
