# AgentWiki 0.10.2 全面复审与发布验收

日期：2026-09-09。基线：已发布0.10.1 / master9b69bc65。范围：原阅读工作区任务、后续代码清理与技术债、前后端与同步包、真实数据库和浏览器；保留既有领域规则。

当前结论：代码与依赖修复均通过独立复审和完整回归，5494项通过（包含单独开启的CodeGraph），仅2项Windows原生测试未运行。主分支复验与发布待完成，本文尚不代表已发布。

## 任务核对

| 原任务或新增发现 | 结果与证据 |
| --- | --- |
| 原阅读工作区Task1–7 | 设计、布局、右上角本文目录、目录细线、位置/历史、Space相关子页与响应式均已完成；本轮继续补测位置与保存并发 |
| 编辑器保存后旧GET覆盖正文/版本 | 175b7bf0：PATCH成功使先前读取失效并取消请求；覆盖保存前及保存中发起GET，保留保存期间新输入 |
| 编辑器离开再POP光标丢失 | 175b7bf0：layout cleanup在handle解绑前记录位置；实际组件卸载/POP回归证明cursor恢复 |
| 并发词法索引旧写覆盖 | 62e92277：先锁Page并读取实时快照，同一短事务发布词法索引 |
| 正文变化后旧向量继续有效 | 62e92277：hash变化在同一事务清空vector；外部embedding在事务外，最终CAS保留，失败后可重试 |
| 目录鉴权与写事务之间降权窗口 | 391f2ecd：五个Web写入口同一事务内按User→Space锁顺序复核实时权限；保留Owner/Editor可写、Admin/Viewer不可写和实时super_admin例外 |
| 大目录写事务超时回归 | be25c772：复审补齐原120秒额度；实际controller执行10000对象rename约14.7秒、54次结构查询，低于60次预算 |
| 依赖安全缺口 | 74f885ed：Hono4.13.5、Multer2.3.0、js-yaml3.15.2/4.3.2、Vitest4.1.11；全依赖与prod审计均0 |
| 技术债与本地清理 | 原0.10.1清理和备份已核对；未改他人worktree与五个参考子模块，未裁剪Mermaid功能来消除构建预算提示 |

目录精确权限依据：`docs/superpowers/specs/2026-08-28-space-folder-hierarchy-design.md`。Reviewer最初将Admin拒绝写视为问题，经规格与测试复核撤回；本次修复没有扩大Admin权限。应用root/server/client统一0.10.2；LocalSync0.9.1、protocol0.6.0保持。

## 多轮审查

1. 任务与代码初审：定位编辑器、搜索两类并发和目录实时权限缺口，先复现再修复。未把推测或仅测试环境配置错误当作产品bug。
2. 分项独立复审：编辑器81/81、独立相关5/5；搜索unit11/11、真实DB5/5；目录unit32/32、真实DB36/36。目录10000对象回归暴露事务默认超时风险，补修后再次通过。
3. 整分支复审9b69bc65..63cfdea1及依赖增量63cfdea1..74f885ed独立复审均Critical0 / Important0 / Minor0；完整回归5493pass / 3skip / 0fail，其中DB207全部运行。
4. 发布前依赖审查发现额外通告；完成升级后复查与复验，测试工具迁移保持原mock reset和dist排除，1426个client、140个protocol、886个LocalSync测试通过；全依赖/prod审计均0。

## 全量验证

| 套件 | 产品修复后完整结果 | 依赖补修后最终结果 |
| --- | --- | --- |
| runtime非DB | 263pass / 1skip | 263pass / 1skip |
| 真实CodeGraph显式补验 | 默认关闭 | 1pass / 0skip；真实本地扫描，远程publish为受控替身 |
| runtime真实DB | 207pass / 0skip | 207pass / 0skip |
| server | 2571pass / 1skip | 2571pass / 1skip |
| client | 1426pass | 1426pass |
| protocol | 140pass | 140pass |
| local-sync | 886pass / 1skip | 886pass / 1skip |
| build / lint / typecheck | 全部通过 | 全部通过 |
| audit | 发现新增依赖通告并补修 | prod/all均0，消除9个不同通告涉及的11个包/版本条目 |

DB目标严格限定本地Docker127.0.0.1:55438/reading_release_test_20260909，Redis127.0.0.1:56388/5；API验收独立使用reading_resume_20260909。专用runner检查容器端口及身份；测试配置不含外部模型凭据。终轮DB零skip。默认runtime跳过的是需显式开启的真实CodeGraph门禁，另两项分别为server Windows OpenCode原生启动和LocalSync Windows ACL验证；不把三项统称平台跳过。CodeGraph本轮另行开启已1/1通过，剩余未执行项仅两条Windows原生验证。

有一次非DB运行与独立build误重叠，server dist暂时被重建导致4个模块缺失；该轮已作废留档，构建完成后顺序重跑263pass/1skip。测试工具升级时旧mock清理语义变化也产生失败，经配置恢复原隔离语义后全量通过；没有删除断言或减少测试发现范围。

## 浏览器与HTTP QA

本地URL `http://127.0.0.1:5188`，API53088。CUA实际窗口808×987；仓库Playwright路由验证1440×1000和390×844。使用本任务合成Space，不操作原“产品知识库”。

| 检查 | 实际观察 |
| --- | --- |
| 目录与创建 | 新Space、目录、进入选中目录后两步创建文章，页面folderId正确；目录浮层、层级与导航正常 |
| 阅读呈现 | 公式KaTeX、代码高亮、Mermaid实际渲染，截图目视确认；checkbox点击后API回读持久化 |
| 编辑/预览/保存 | 未保存预览、返回编辑、保存仍留编辑、刷新后正文保留；迟到GET与POP光标另由真实组件回归覆盖 |
| 搜索 | 输入文章标题，结果摘要为最新AUDIT-ROUND2-FIXED-SAVE正文；点击进入最新文章，无console错误 |
| 长文/历史/图谱 | 本文目录定位并收起；历史v2预览旧正文后回编辑；图谱选中节点后链接返回文章 |
| 相关Space子页 | 协作模板向导到Agent步骤，空Agent提示清楚；设置描述保存并刷新保留；来源/运行/成员/模板由路由套件覆盖 |
| 路由/响应式 | 5公开+15认证+7移动路由，另7旧路由重定向；无浏览器错误、API5xx或水平溢出 |
| HTTP/MCP | 32项：认证、空间、页面CRUD/版本、Syncv3能力、Agent凭据/MCP权限与审核等通过 |
| 目录真实HTTP | Editor创建/改名/移动/删除/恢复成功；Admin/Viewer403且treeRevision不变，共7项 |

离开编辑时实际出现确认提示；本轮未成功记录显式“取消离开”的GUI动作，不将其列为新GUI证据，既有自动化回归仍覆盖。空Agent提示不等于外部Agent完整运行成功。

本地截图与全部原始日志：`/Users/neomei/.codex/recovery/agentwiki-full-audit-20260909/`；`final/ui-route-desktop-editor.png`、`final/ui-route-mobile-page.png`、`ui-reading-round2.png`。临时脚本与凭据不进入Git。本轮人工Space、Page已通过API删除后404复核；原产品知识库200保留。依赖补修后的路由、HTTP32项、目录HTTP7项再次全部通过。

## 发布与清理

- 发布前只读生产检查：1055源码输入与9b69bc65一致；56个已应用迁移校验和一致，0pending/unresolved；239活跃页面的词法文档0缺失/0陈旧。
- 0.10.2尚未部署。备份、部署、静态资源/env哈希、公网认证验收、精确fixture清理与Git状态待补。

## 证据边界

覆盖范围内最后一轮无已确认且未修复的产品问题，不能证明任意环境下绝对零bug。生产灾难恢复、Windows原生、独立Obsidian插件实际Vault、外部付费模型/Agent完整流程未在本轮重跑。同步包未发布npm，已安装外部客户端不会因仓库依赖升级而自动更新。历史semantic vector未强制重新生成，无法仅凭新hash识别部署前已存在的旧vector；本修复保证后续索引一致性。完整Mermaid上游解析器690864字节保留720000字节懒加载预算。
