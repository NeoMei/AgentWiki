# 本地代码整理与前端技术债处理

2026-09-09。基线 `9fc3fe5f`，分支 `codex/workspace-cleanup-tech-debt-20260909`。本轮仅本地整理及代码集成；已发布的 v0.10.0 tag、GitHub Release 和生产部署保持原状态，没有另行发布版本。

## 本地工作区

- 五个参考子模块的4969个文件逐一核对，差异仅为CRLF/LF；先按原始字节归档，再恢复Git记录的内容。另一个内容本来一致的文件刷新Git索引状态。五个子模块均无跟踪文件差异。
- 原47个未跟踪文件全部备份并校验SHA256。错误根目录下的旧迁移副本（缺少正式版本末尾的索引）和临时复现文件移入私有归档；正式迁移完全不变。
- 其余45份历史报告、图片、会话记录、锁文件和本地索引保留原位，通过本地 `.git/info/exclude` 管理；没有删除或提交这些用户资料。openwiki 的本地索引同样使用本地忽略规则。
- 私有备份、原忽略规则及文件清单：`/Users/neomei/.codex/recovery/agentwiki-code-cleanup-20260909/`。恢复时可按清单取回原始字节；会话锁文件未移动。

## 测试入口

`pnpm --filter @agentwiki/client test [Vitest参数]` 现在先构建 shared 与 sync-protocol，再执行 Vitest。依赖构建失败时由 `&&` 阻止测试继续，筛选参数仍传给 Vitest。

- 故意将shared生成物中的空间名长度改为旧值10，原测试入口产生5项失败（41项中的36项通过）；修改入口后相同41项全部通过，源码限制32保持不变。
- 将两个依赖的dist目录移出后直接运行标准客户端测试，自动重建成功，1422项全部通过。
- 后续增加预算混合模块回归，最终103个测试文件、1423项全部通过，无跳过。

## 前端加载

将工作区及相关页面按路由延迟加载，保留原Provider、导航守卫和工作区状态。React、编辑器核心、公式、图表布局和Mermaid共享模块按依赖边界分块，没有替换编辑器或削减图表类型。

| 未压缩生产JavaScript | 之前 | 之后 |
| --- | ---: | ---: |
| 首屏静态依赖总量 | 约927.36 KB | 494.791 KB |
| 入口文件 | 约927.36 KB | 254.235 KB |
| PageEditor分块 | 约710.80 KB | 466.195 KB |
| Markdown分块 | 约699.57 KB | 409.300 KB |

首屏总量减少约46.6%。这反映加载边界变化，不能表述为整个应用下载量减少同样比例。重功能仍在访问相关页面时加载。

生产构建强制预算：首屏静态依赖总量550000字节，普通单块500000字节；编辑器、公式、图表运行库不得进入首屏。唯一例外是完全由Mermaid上游完整解析器模块组成的懒加载块，限制720000字节，当前690864字节。保留Vite原有大块警告，不提高全局警告阈值；使用tiny版会丢失图表类型，故未采用。

预算守卫在Vite写入动态预加载映射后执行，输出494791字节，与独立读取磁盘文件、遍历静态导入所得结果一致。6项预算回归覆盖静态依赖去重、累计超限、单块超限、受控解析器例外、重库误入首屏和解析器混入应用代码。

## 验证与审查

- 最终客户端1423/1423通过；客户端lint、全仓typecheck、生产构建及 `git diff --check` 通过。构建工具额外eslint/typecheck通过（使用server已有Node类型定义做独立编译检查）。
- 独立只读审查发现并修复两项预算漏检：混合应用模块被解析器例外误放行、统计早于Vite最终代码重写。复审C0/I0/M0。
- 真实生产构建运行于 `http://127.0.0.1:5188`，连接原隔离API53088和合成数据库。验证编辑→保存→预览→阅读→勾选任务，API回读确认内容及 `[x]` 持久化。
- 公式、JavaScript语法高亮、流程图、时序图、类图、饼图、架构图全部真实渲染为预期元素/SVG。用于检查拆包后Mermaid模块依赖环是否造成实际初始化错误；所测类型无运行错误。
- 空间设置、成员、工作台、个人资料、搜索、使用指南、Agent接入、Obsidian指南及5个文档页面逐页真实打开。390px目录抽屉、右上本文目录浮窗正常，页面无横向溢出；临时视口已复原。
- 本轮合成测试页已通过原接口软删除并GET404复核。浏览器没有相关console错误或警告。截图、路由检查、保存回读、构建字节和测试日志保存在上述私有证据目录。

后台业务源码、数据库迁移、权限、目录修订、CAS保存、同步协议和已发布包版本无变更。本轮没有重跑服务端数据库全套测试、Windows原生验证、全部Mermaid图表类型或外部Agent服务；上轮发布的完整5454项结果仍只代表v0.10.0发布基线。
