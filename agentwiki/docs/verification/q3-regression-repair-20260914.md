# Q3 人工回归修复候选（2026-09-14）

用户授权“那修吧”；本轮修复主应用与独立 Obsidian 插件，不含发布、生产部署或日常 Vault 安装。原报告 20 项均有处置；新 3、新 4 的原始 Windows 现场仍需补验，不能按便携测试结果关闭。

主仓基线 `166383e3118587ad6eb47ec272277221bec77111`，插件基线 `57514242287c763837d7e1472909eff4b88ded0d`；两仓修复分支均为 `codex/q3-fixes-20260914`。

## 逐项回测

| 原编号 | 本轮处理 | 测试员回测重点 |
| --- | --- | --- |
| 旧001 | 强制改密期间停止导航审核计数轮询 | 重置密码后停留改密页超过5秒，完成改密，正常会话继续有效 |
| 旧002 | 接入指南替换过时凭证截图，按当前入口说明 | 从 Agent Access 选空间和角色，生成并复制完整接入指令 |
| 旧003 | 修复 Git blobless inventory 超时；提供分阶段安全错误码 | 同一 taste-skill 地址重新获取，失败时展开运行查看可操作提示 |
| 旧004 | 系统协作任务名称/目标展示双语 | 切换语言；自定义模板和用户正文应保持原文 |
| 旧005 | 成功 Toast 3秒关闭，错误保留 | 连续复制并观察关闭；错误仍可手工关闭 |
| 旧007 | 权限错误按状态/错误码本地化 | 被移除成员后访问页面仍被拒绝，中文错误可读 |
| 旧08 | 图谱缩放、平移、节点拖动、适应和重置 | 桌面鼠标及触摸操作，拖动后不误选其他节点 |
| 旧0009 | Agent 角色显示本地化 | 中文页面显示角色译名，权限边界不变 |
| 旧010 | 明确一凭证绑定一个空间授权 | 为其他空间使用对应接入凭证；当前连接不会聚合全部 Grant |
| 旧011 | 回滚带权威 treeRevision，失败不盲目重试 | 普通变更集回滚；同时修改目录时提示冲突；v3限制仍保留 |
| 旧012 | 同候选及同空间既有页面的完全相同非空正文提示 | 展开候选查看示例页面链接；允许合法重复，不强制阻止发布 |
| 旧013 | 角色ID与输入参数采用稳定编辑行身份 | 连续输入、粘贴、删除前一行后继续编辑，不失焦 |
| 新1 | 标题输入有标签、边框、空态和聚焦样式 | 空标题区域清晰可见并可编辑 |
| 新2 | HTTP/MCP/知识包及服务写入边界禁止新增或改为纯空白标题；历史空标题显示占位且可点 | 新建/保存空白被拒绝，既有异常页可进入编辑；不批量改旧正文 |
| 新3 | 控制 payload 形状/哈希/版本诊断分开；无效写入不污染 .next | 原现场待实际插件版本与脱敏控制诊断、隔离 Windows Vault 复测；不删除控制文件 |
| 新4 | 补齐 v3 校验总正文容量，输出对象和字段级差异 | 原 V3_VAULT_VERIFY_FAILED 待原生复测；保留完整性检查和失败证据 |
| 新5 | v2 明确“按服务器恢复”，与自动合并删除分开 | 删除2目录5页后选择恢复；预览后编辑应拒绝，未托管PDF/图片应保留 |
| 新6 | 合并文字与语义检索，文字命中不显示虚构100% | 标题精确/子串及正文命中；无权和已删除页面不得泄露 |
| 新7 | 缺失/过期候选基线拒绝发布，审核与发布单事务 | 候选提交后人工保存，再审核发布必须冲突；原正文、历史和审核状态不能半提交 |
| 新8 | 管理协作模板进入协作模板库 | 与页面模板入口区分，复制/编辑/归档流程正常 |

## 已取得的独立证据

- 报告中的公开仓库 `https://github.com/Leonxlnx/taste-skill`：原完整 SourceService.fetch 在 inventory 阶段超时；修复后完整获取约1601毫秒，33文件、33片段、369469字节，仓库提交 `ccbc15639c97057cbfcf32ecebc38ef716e4bb37`。这不是原生产失败运行重放；生产SSH只读连接未成功。
- 真实 Chrome + 隔离 API fixture：7条流程通过（强制改密、角色连续输入、回滚请求、搜索标签、图谱、标题校验、空标题目录）；图谱覆盖1365×900与390×844、鼠标及CDP触摸事件。不是生产端到端或Windows原生验收。
- 浏览器原始结果和截图：`/Users/neomei/.codex/recovery/agentwiki-q3-20260914/`。隔离测试数据库为本地任务专用 pgvector 容器，无生产数据库写入。
- 协议140项通过；Local Sync 924项通过/1项跳过；运行时263项通过/1项CodeGraph门控跳过。主应用最终完整构建通过；服务端155套/2663项通过、4项跳过；前端108文件/1503项通过。主仓typecheck/lint通过。插件68文件/1385项通过，完整check（类型、格式、构建、包安全）通过，lint为0错误/19警告。

## 验收边界

未发布、未部署、未安装日常 Vault、未执行受影响 Windows 原生客户端。新3/4需要测试者实际版本及不含凭证和正文的诊断；已知代码缺陷修复和便携测试不等于历史截图根因已确认。单凭证单空间为现有授权边界，重复正文为建议提示，自动合并保留删除语义。

## 审查记录

各任务独立审查覆盖规格与代码；已修正图谱兼容鼠标事件误选、Git错误码误判、文字搜索无界正文加载、插件恢复预览过期及非托管后代删除及批量查重串行扫描等问题。500篇不同正文一次集合查重的单元/真实数据库回归通过；最终增量server build/typecheck/lint通过。最终复审及整分支结果待补录。检查日志与详细审查报告位于工作树`.superpowers/q3/`，其中初次服务端测试遭任务数据库中断的失败日志仍保留；恢复数据库后最终全量通过。

整分支复审追加修复：跨入口空白标题使用共同规则，覆盖MCP实际注册schema与handler、知识包、PageService、提案和发布；真实数据库验证新建/改名拒绝且审批不半提交，历史空白标题的content-only发布仍可成功。入口红阶段7失败/24通过，修复后全服务端2663通过/4跳过；server build/typecheck及主仓lint通过。普通V2隐藏目录事务已统一持久化枚举模式；日志读取器允许该布尔字段，仍拒绝错误类型与未知字段，完整插件check为1385项通过。

## 本地保存结果

主应用候选已保存于分支 `codex/q3-fixes-20260914`：`d332c6d8`（代码与验收材料）、`ce44190c`（记忆归档）。独立插件候选已保存于同名分支：`5c22581`。这些提交只存在本机，未推送、未发布、未部署。

最终整分支独立审查为 CLEAN；验证对象的最终源码清单和 SHA-256 保存在 `/Users/neomei/.codex/recovery/agentwiki-q3-20260914/source-manifest-final.json`。
