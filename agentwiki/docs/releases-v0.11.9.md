# AgentWiki v0.11.9

Taskboard fidelity fix found by side-by-side comparison against the original plugin (same data, local serve).

- Canvas node stage signals read 验证/实现/验收 (previously 前置/功能/集成), matching the project-taskboard original.
- Client-only change; Local Sync and sync protocol versions remain 0.10.0 and 0.6.0.
