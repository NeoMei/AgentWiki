import React from 'react';
import type { CollaborationRun } from '../types';

export const AgentActivityPanel: React.FC<{ run: CollaborationRun; t: (key: string) => string; onHistory: () => void }> = ({ run, t, onHistory }) => <section data-testid="dashboard-section-activity" className="order-5 min-w-0 rounded-xl border bg-white p-4 lg:col-start-3 lg:row-start-3"><h2 className="font-semibold">{t('collaboration.dashboard.activity')}</h2><ol className="mt-3 space-y-3">{(run.events ?? []).length ? [...(run.events ?? [])].reverse().map((event) => <li key={event.id} className="border-l-2 border-blue-200 pl-3"><p className="break-words text-sm font-medium">{t(`collaboration.event.${event.operation}`)}</p><p className="mt-1 text-xs text-gray-500">#{event.sequence} · {t(`collaboration.actor.${event.actorKind}`)} · {new Date(event.createdAt).toLocaleString()}</p></li>) : <li className="text-sm text-gray-500">{t('collaboration.dashboard.noActivity')}</li>}</ol><button type="button" onClick={onHistory} className="mt-4 min-h-9 rounded-lg border px-3 text-sm">{t('collaboration.dashboard.viewAllActivity')}</button></section>;
